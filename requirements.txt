# Core data science libraries
pandas>=1.5.0
numpy>=1.21.0
# Pinned: the committed models/resume_clf.joblib is pickled with this version.
# A mismatch triggers sklearn's InconsistentVersionWarning and risks a bad load.
# If you retrain (python train.py) on a newer sklearn, bump this to match.
scikit-learn==1.9.0
joblib>=1.2.0

# Natural language processing / embeddings
sentence-transformers>=2.2.0

# Retrieval-augmented generation
faiss-cpu>=1.7.0
anthropic>=0.40.0
python-dotenv>=0.19.0

# PDF processing
pdfplumber>=0.7.0

# Job-posting fetching (screener)
requests>=2.28.0
beautifulsoup4>=4.11.0
lxml>=4.9.0

# Web interface
streamlit>=1.25.0
streamlit-pdf-viewer>=0.0.1

# Notebook-only (semantics.ipynb) exploration/plots
matplotlib>=3.5.0
seaborn>=0.11.0
