# Copy to `.env` (git-ignored) and fill in your key, OR set as a real env var.
# Enables the AI Fit Analysis (RAG) tab. Retrieval works without it.
# Get a key at https://console.anthropic.com — one key works for all models.
ANTHROPIC_API_KEY=sk-ant-...
